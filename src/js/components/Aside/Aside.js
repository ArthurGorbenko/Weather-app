import { Component } from "../../framework";

export default class Aside extends Component {
    constructor(host,props) {
        super(host,props);
    }

    render() {
        return "I am ASide !"
    }
}